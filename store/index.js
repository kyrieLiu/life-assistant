/**
 * @author Liu Yin
 * @date 2020/2/10
 * @Description: 状态管理
 */
// import {state} from './app'
const getters = {
  // sidebar: state => appState.sidebar
}
const actions = {
  nuxtServerInit ({ commit }, { req }) {
  }
}
export default {
  getters,
  actions
}
